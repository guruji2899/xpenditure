<%= link_to 'New Account', new_account_path, :class => "btn btn-success pull-right"  %>
<h1>Listing accounts</h1>
<div class="table-responsive">
<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Balance</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @accounts.each do |account| %>
      <tr>
        <td><%= account.name %></td>
        <td><%=  %></td>
        <td><%= link_to 'Show', account , :class => "btn btn-info"%>
        <%= link_to 'Edit', edit_account_path(account) , :class => "btn btn-primary"%>
        <%= link_to 'Destroy', account, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>

<br>


